package Leetcode_String;

public class VectorDemo {
	/*
	 * Vector类实现动态数组。和ArrrayList很相似，但两者有不同。
	 * Vector是同步访问
	 * Vector包含了许多传统方法，这些方法不属于集合框架。
	 * Vector主要用于事先不知道数组的大小，或者只是需要一个可以改变大小的数组的情况
	 * 
	 * 
	 */

}
